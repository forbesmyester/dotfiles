% ndjson-env

# Env vars using `. $(navi --print)`
ndjson-env -f <filename> <envname>

$ filename: find . -maxdepth 1 -type f --- --column 1
$ envname: ndjson-env -f "$filename" | grep '^ \*' | sed 's/^ \* //' --- --column 1
