#!/bin/bash

REQUESTED_DESKTOP=$2
DESKTOP_CONTEXT_SIZE=$1
CURR=$(wmctrl -d | grep '^[0-9]\+\s\+\*' | sed 's/\s.*//')
CURRSET=$(expr $CURR / $DESKTOP_CONTEXT_SIZE)
FINAL_DESKTOP=$(((CURRSET * $DESKTOP_CONTEXT_SIZE) + $REQUESTED_DESKTOP))
echo $FINAL_DESKTOP
wmctrl -s $FINAL_DESKTOP
